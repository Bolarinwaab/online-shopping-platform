const http=require('node:http');const {loadConfig}=require('./config');
function startServer({port=loadConfig().port}={}){return http.createServer((req,res)=>{const id=req.headers['x-correlation-id']||require('node:crypto').randomUUID();res.setHeader('X-Correlation-ID',id);res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('Referrer-Policy','no-referrer');res.setHeader('X-Frame-Options','DENY');if(req.url==='/healthz'||req.url==='/readyz'){res.writeHead(200,{'Content-Type':'application/json'});return res.end(JSON.stringify({status:req.url==='/healthz'?'ok':'ready'}));}res.writeHead(404,{'Content-Type':'application/json'});res.end(JSON.stringify({error:'not_found',correlationId:id}));}).listen(port);}
if(require.main===module)startServer();module.exports={startServer};
